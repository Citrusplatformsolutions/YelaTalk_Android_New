package com.kainat.app.android.bean;


public class CommunityFav {
//	if(entry.browseMsgUrl != null && entry.browseMsgUrl.length() > 5){
//		DataModel.sSelf.storeObject(DMKeys.USER_PROFILE, entry.groupName);
//		DataModel.sSelf.storeObject(DMKeys.XHTML_URL, entry.browseMsgUrl);
//		intent = new Intent(CommunityActivity.this, WebviewActivity.class);
//		intent.putExtra("from_community", true);
//		startActivity(intent);
//	}else if(entry.messageUrl != null && entry.messageUrl.length() > 5){
//	intent = new Intent(CommunityActivity.this, CommunityChatActivity.class);
//	intent.putExtra("message_url", entry.messageUrl);
//	intent.putExtra("group_name", entry.groupName);
	
	
}
